<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainMenuButton" parent="CircularButton">
        <item name="android:layout_width">120sp</item>
        <item name="android:layout_height">120sp</item>
        <item name="android:layout_marginBottom">5sp</item>
        <item name="android:layout_marginLeft">10sp</item>
        <item name="android:layout_marginRight">10sp</item>
        <item name="android:layout_marginTop">10sp</item>
    </style>

    <style name="CircularButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/btn_background</item>
        <item name="at.fourtytwo.wordclock:font_name">fontawesome.ttf</item>
    </style>

    <style name="FontAwsomePath">
        <item name="at.fourtytwo.wordclock:font_name">fontawesome.ttf</item>
    </style>
</resources>
